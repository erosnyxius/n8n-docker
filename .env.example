# ==========================================
# 1. CONTAINER IDENTITY & PERFORMANCE
# ==========================================
# The name that appears in Docker (e.g., n8n-main)
CONTAINER_NAME=n8n-main

# 'filesystem' prevents RAM crashes with large files
N8N_DEFAULT_BINARY_DATA_MODE=filesystem

# ==========================================
# 2. DATABASE (Postgres)
# ==========================================
POSTGRES_USER=n8n
POSTGRES_PASSWORD=change_this_password_immediately
POSTGRES_DB=n8n

# ==========================================
# 3. SECURITY & APP CONFIG
# ==========================================
# Generate keys: openssl rand -hex 32
N8N_ENCRYPTION_KEY=replace_with_random_string_32_chars
N8N_RUNNERS_TOKEN=replace_with_random_string_32_chars

GENERIC_TIMEZONE=Asia/Dhaka

# ==========================================
# 4. INFRASTRUCTURE (Choose Local or Prod)
# ==========================================

# --- OPTION A: LOCAL (HTTP / No Domain) ---
# DOMAIN_NAME=:80
# PORT=8080
# SSL_PORT=4443
# WEBHOOK_URL=http://localhost:8080/
# N8N_SECURE_COOKIE=false

# --- OPTION B: PROD (HTTPS / Domain) ---
# DOMAIN_NAME=n8n.your-domain.com
# PORT=80
# SSL_PORT=443
# WEBHOOK_URL=https://n8n.your-domain.com/
# N8N_SECURE_COOKIE=true

# ==========================================
# 5. OFFSITE BACKUPS (Filebase - 5GB Free S3)
# ==========================================
# Filebase: Free 5GB S3-compatible storage
# Sign up: https://filebase.com (works with n8n S3 node!)
S3_ENABLED=false
S3_BUCKET=your-bucket-name
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_ENDPOINT=https://s3.filebase.com